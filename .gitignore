# Local council output (regenerated per run)
.harness/last_council.md

# Circuit breaker trigger file (local only)
.harness_halt

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Node dev-dependency artifacts (Phase 2 adds package.json for tests/lint only;
# nothing from node_modules is required at runtime and none of it ships to the
# SharePoint-synced folder).
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
